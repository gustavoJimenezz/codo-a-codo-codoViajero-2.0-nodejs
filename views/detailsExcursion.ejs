<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= excursion.name %> - CodoViajero</title>
  <meta name="description" content="<%= excursion.detailsExcursion ? excursion.detailsExcursion.description : excursion.description %>">
  <link rel="stylesheet" href="/css/styles.css">
  <link href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    #menu-toggle:checked + #menu {
      display: block;
    }
  </style>
</head>

<body class="min-h-screen flex flex-col bg-gray-50">
  <!-- Hero Section con imagen de fondo -->
  <header class="relative">
    <%- include('./layouts/partials/nav') %>

    <div class="relative h-72 md:h-96 overflow-hidden">
      <% if (imagesExcursions && imagesExcursions.length > 0) { %>
        <img
          src="/img/excursions/excursion-cordoba-1/<%= imagesExcursions[0].img %>"
          alt="<%= excursion.name %>"
          class="absolute inset-0 w-full h-full object-cover"
        >
      <% } %>
      <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/40 to-transparent"></div>

      <div class="absolute bottom-0 left-0 right-0 p-6 md:p-10">
        <div class="container mx-auto max-w-screen-xl">
          <nav class="flex items-center gap-2 text-sm text-white/80 mb-4" aria-label="Breadcrumb">
            <a href="/" class="hover:text-white transition-colors">Inicio</a>
            <span>/</span>
            <a href="/excursions" class="hover:text-white transition-colors">Excursiones</a>
            <span>/</span>
            <span class="text-white"><%= excursion.name %></span>
          </nav>
          <h1 class="text-3xl md:text-5xl font-bold text-white">
            <%= excursion.name %>
          </h1>
        </div>
      </div>
    </div>
  </header>

  <main class="flex-grow">
    <div class="container mx-auto max-w-screen-xl px-4 py-8 md:py-12">

      <% if (!hasDetails) { %>
        <div class="flex items-center gap-3 p-4 mb-8 text-sm text-yellow-800 rounded-lg bg-yellow-50 border border-yellow-200" role="alert">
          <svg class="w-5 h-5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
          </svg>
          <span><strong>Atención:</strong> No se encontraron detalles para esta excursión. Algunos datos pueden no estar disponibles.</span>
        </div>
      <% } %>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">

        <!-- Columna principal -->
        <div class="lg:col-span-2 space-y-8">

          <!-- Galería de imágenes -->
          <% if (imagesExcursions && imagesExcursions.length > 0) { %>
            <section>
              <div id="excursion-carousel" class="relative w-full" data-carousel="slide">
                <div class="relative h-64 md:h-[400px] overflow-hidden rounded-xl shadow-lg">
                  <% imagesExcursions.forEach((image, index) => { %>
                    <div class="hidden duration-700 ease-in-out" data-carousel-item="<%= index === 0 ? 'active' : '' %>">
                      <img
                        src="/img/excursions/excursion-cordoba-1/<%= image.img %>"
                        class="absolute block w-full h-full object-cover"
                        alt="<%= excursion.name %> - Imagen <%= index + 1 %>"
                      >
                    </div>
                  <% }); %>
                </div>

                <% if (imagesExcursions.length > 1) { %>
                  <button type="button" class="absolute top-1/2 -translate-y-1/2 left-3 z-30 flex items-center justify-center w-10 h-10 rounded-full bg-white/80 hover:bg-white shadow-md transition-all group" data-carousel-prev aria-label="Imagen anterior">
                    <svg class="w-5 h-5 text-gray-800" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
                    </svg>
                  </button>
                  <button type="button" class="absolute top-1/2 -translate-y-1/2 right-3 z-30 flex items-center justify-center w-10 h-10 rounded-full bg-white/80 hover:bg-white shadow-md transition-all group" data-carousel-next aria-label="Imagen siguiente">
                    <svg class="w-5 h-5 text-gray-800" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                    </svg>
                  </button>

                  <!-- Indicadores -->
                  <div class="absolute bottom-4 left-1/2 -translate-x-1/2 flex gap-2">
                    <% imagesExcursions.forEach((_, index) => { %>
                      <button type="button" class="w-2.5 h-2.5 rounded-full bg-white/50 hover:bg-white transition-colors" data-carousel-slide-to="<%= index %>" aria-label="Ir a imagen <%= index + 1 %>"></button>
                    <% }); %>
                  </div>
                <% } %>
              </div>
            </section>
          <% } %>

          <!-- Descripción -->
          <section class="bg-white rounded-xl p-6 shadow-sm">
            <h2 class="text-xl font-semibold text-gray-900 mb-4">Sobre esta excursión</h2>
            <p class="text-gray-600 leading-relaxed">
              <%= excursion.detailsExcursion ? excursion.detailsExcursion.description : excursion.description %>
            </p>
          </section>

          <!-- Detalles adicionales -->
          <section class="bg-white rounded-xl p-6 shadow-sm">
            <h2 class="text-xl font-semibold text-gray-900 mb-4">Información del viaje</h2>
            <dl class="grid grid-cols-1 sm:grid-cols-2 gap-4">
              <div class="flex items-center gap-3 p-4 bg-gray-50 rounded-lg">
                <div class="flex-shrink-0 w-10 h-10 flex items-center justify-center bg-orange-100 rounded-full">
                  <svg class="w-5 h-5 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                  </svg>
                </div>
                <div>
                  <dt class="text-sm text-gray-500">Duración</dt>
                  <dd class="font-semibold text-gray-900">
                    <%= excursion.detailsExcursion ? excursion.detailsExcursion.duration + ' horas' : 'No disponible' %>
                  </dd>
                </div>
              </div>

              <div class="flex items-center gap-3 p-4 bg-gray-50 rounded-lg">
                <div class="flex-shrink-0 w-10 h-10 flex items-center justify-center bg-orange-100 rounded-full">
                  <svg class="w-5 h-5 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                  </svg>
                </div>
                <div>
                  <dt class="text-sm text-gray-500">Destino</dt>
                  <dd class="font-semibold text-gray-900">
                    <%= excursion.destination ? excursion.destination.name : 'Argentina' %>
                  </dd>
                </div>
              </div>
            </dl>
          </section>
        </div>

        <!-- Sidebar - Tarjeta de reserva -->
        <aside class="lg:col-span-1">
          <div class="sticky top-6 bg-white rounded-xl shadow-lg overflow-hidden">
            <div class="bg-gradient-to-r from-orange-500 to-orange-600 p-6 text-white">
              <p class="text-sm font-medium opacity-90">Precio por persona</p>
              <p class="text-4xl font-bold mt-1">
                $<%= excursion.detailsExcursion ? excursion.detailsExcursion.price : 'N/A' %>
              </p>
            </div>

            <div class="p-6 space-y-4">
              <div class="flex items-center gap-3 text-sm text-gray-600">
                <svg class="w-5 h-5 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                </svg>
                <span>Cancelación gratuita</span>
              </div>
              <div class="flex items-center gap-3 text-sm text-gray-600">
                <svg class="w-5 h-5 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                </svg>
                <span>Reserva ahora, paga después</span>
              </div>
              <div class="flex items-center gap-3 text-sm text-gray-600">
                <svg class="w-5 h-5 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                </svg>
                <span>Guía en español</span>
              </div>

              <hr class="my-4">

              <a href="/auth/login" class="block w-full py-3 px-6 text-center text-white font-semibold bg-orange-600 hover:bg-orange-700 rounded-lg transition-colors">
                Reservar ahora
              </a>

              <p class="text-xs text-center text-gray-500 mt-3">
                Confirma disponibilidad sin compromiso
              </p>
            </div>
          </div>
        </aside>
      </div>

      <!-- Disponibilidad -->
      <% if (availability && availability.length > 0) { %>
      <div class="mt-6">
        <h3 class="text-xl font-semibold text-gray-900 mb-4">Disponibilidad</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          <% availability.forEach(function(slot) { %>
          <div class="p-4 border border-gray-200 rounded-lg bg-gray-50">
            <% if (slot.date) { %>
            <p class="text-sm text-gray-600">
              <span class="font-medium">Fecha:</span>
              <%= new Date(slot.date).toLocaleDateString('es-AR') %>
            </p>
            <% } %>
            <p class="text-sm text-gray-600">
              <span class="font-medium">Hora de inicio:</span>
              <%= slot.start_time %>
            </p>
            <p class="text-sm text-gray-600">
              <span class="font-medium">Duración:</span>
              <%= slot.duration %> horas
            </p>
          </div>
          <% }); %>
        </div>
      </div>
      <% } %>
    </div>
  </main>

  <footer class="mt-auto">
    <%- include('./layouts/partials/footer') %>
  </footer>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.6.3/flowbite.min.js"></script>
</body>

</html>
