<!DOCTYPE html>
<html lang="es">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CodoViajero - Registro</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  </head>

  <body class="w-screen h-screen bg-gradient-to-t from-codo-blue-dark/90 via-codo-blue/80 to-brand-orange">
    <div class="flex items-center justify-center h-full py-8">
      <div class="flex flex-col justify-center items-center gap-6 border-box w-1/3 min-w-96 p-10 rounded-lg bg-gradient-to-b from-codo-blue-dark/90 via-codo-blue/80 to-brand-orange/60">

        <div class="@container w-full">
          <!-- Include the logo -->
          <%- include('../layouts/partials/logoCodigoViajero.ejs') %>
        </div>

        <form action="/user" method="POST" class="flex flex-col space-y-1 gap-2 w-full max-w-80">

          <input
            value="<%= (typeof values !== 'undefined' && values.name) ? values.name : '' %>"
            type="text"
            id="name"
            name="name"
            class="p-3 border-[1px] border-slate-500 rounded-md w-full"
            placeholder="Name (min. 3 characters, letters only)">

          <input
            value="<%= (typeof values !== 'undefined' && values.lastName) ? values.lastName : '' %>"
            type="text"
            id="lastName"
            name="lastName"
            class="p-3 border-[1px] border-slate-500 rounded-md w-full"
            placeholder="Last Name (min. 3 characters)">

          <input
            value="<%= (typeof values !== 'undefined' && values.email) ? values.email : '' %>"
            type="email"
            id="email"
            name="email"
            class="p-3 border-[1px] border-slate-500 rounded-md w-full"
            placeholder="E-Mail">

          <input
            type="password"
            id="password"
            name="password"
            class="p-3 border-[1px] border-slate-500 rounded-md w-full"
            placeholder="Password (min. 6 characters)">

          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            class="p-3 border-[1px] border-slate-500 rounded-md w-full"
            placeholder="Confirm Password">

          <div class="flex flex-col space-y-5 w-full pt-4">
            <button type="submit" class="w-full bg-[#0070ba] rounded-3xl p-3 text-white font-bold transition duration-200 hover:bg-[#003087]">Register</button>
            <div class="flex items-center justify-center w-full relative">
              <div class="-mt-1 font-bold px-5 absolute text-white">Or</div>
            </div>
            <button type="button" class="text-white hover:text-[#0070ba] w-full border-white hover:border-[#003087] hover:border-[2px] border-[1px] rounded-3xl p-3 font-bold transition duration-200">
              <a href="/auth/login">Log in</a>
            </button>
          </div>
        </form>

        <% if (typeof validations != 'undefined' && validations.length > 0) { %>
          <% validations.forEach(function(validation) { %>
            <div role="alert" class="rounded border-s-4 border-red-500 bg-red-50 p-4 w-full">
              <div class="flex items-center gap-2 text-red-800">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="size-5">
                  <path fill-rule="evenodd" clip-rule="evenodd" d="M9.401 3.003c1.155-2 4.043-2 5.197 0l7.355 12.748c1.154 2-.29 4.5-2.599 4.5H4.645c-2.309 0-3.752-2.5-2.598-4.5L9.4 3.003zM12 8.25a.75.75 0 01.75.75v3.75a.75.75 0 01-1.5 0V9a.75.75 0 01.75-.75zm0 8.25a.75.75 0 100-1.5.75.75 0 000 1.5z"/>
                </svg>
                <strong class="block font-medium"><%= validation.path %></strong>
              </div>
              <p class="mt-2 text-sm text-red-700"><%= validation.msg %></p>
            </div>
          <% }); %>
        <% } %>

      </div>
    </div>
  </body>
</html>
